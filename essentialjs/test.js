const BIG_NUMBER = 100;

const bigArray = Array(BIG_NUMBER);

const dumbProcess = () => {
  console.log('bigArray : ', bigArray);
};

const main = () => {
  dumbProcess();
  // big Array를 쓰지 않는 작업 100만개
};
